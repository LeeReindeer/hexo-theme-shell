<!DOCTYPE html>
<div>
    <h1 class="text-center site_title">
        <%=config.title%>
    </h1>
    <p class="text-center text-muted">
    <%if (theme.memo){%>
    <%=theme.memo%>
    <%} else{%>
     
    <%}%>
    </p>
</div>
<hr>
<div class="row content">
    <% page.posts.each(function(post){ %>
        <%- partial('_partial/article-excerpt', {post: post}) %>
    <%})%>
</div>
